# Dependencias de ejecución - Etapa 1
# Versiones fijadas para que el entorno sea reproducible entre máquinas.
# NO se incluye PyTorch, MediaPipe ni ningún modelo de IA: entran en Fase 2/3
# en su propio archivo de requisitos para no inflar la instalación base.

fastapi==0.115.6
uvicorn[standard]==0.34.0

pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0   # requerido por pydantic.EmailStr

SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

python-multipart==0.0.20
bcrypt==4.2.1

# --- Etapa 2 ---
# Migraciones de esquema. Sustituye a `create_all`, que solo crea tablas
# nuevas y no aplica cambios a las que ya tienen datos.
alembic==1.19.2

# Tokens JWT. Se elige PyJWT sobre `python-jose` (el de la documentacion
# antigua de FastAPI): python-jose tiene mantenimiento irregular e historial
# de CVEs. PyJWT es Python puro, sin compilacion ni dependencias pesadas.
PyJWT==2.13.0

# --- Fase 1 ---
# Tratamiento de imagenes. Se gana el sitio dos veces:
#   1. Valida que un archivo subido sea REALMENTE una imagen, abriendolo, en
#      vez de fiarse de la cabecera Content-Type que envia el cliente.
#   2. Compone la prenda sobre la foto en el proveedor local de vista previa.
Pillow==12.3.0
